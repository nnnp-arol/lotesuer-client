import { LinkProps, SvgIconTypeMap } from "@mui/material";
import { OverridableComponent } from "@mui/material/OverridableComponent";
import * as React from "react";

export type AppTemplateType = {
  children: React.ReactNode;
};
